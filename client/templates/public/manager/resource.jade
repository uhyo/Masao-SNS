section
  h1= name
  - time = new Date(uptime)
  p
    | アップロード者:
    a(href="/user/id/#{user._id}")= user.name
    | (
    include ../../time
    | )
  figure.preview
  section
    h2 情報の変更
    p ファイルをアップロードしなおす場合はファイルを選択して下さい。
    .formarea
  .dataarea(hidden)
    p ファイルサイズ:#{size}Bytes
    p= comment
    
